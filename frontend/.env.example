# Base URL of the backend API this widget talks to (GET /services,
# GET /availability, POST /bookings). Never hardcode this -- it's what lets
# the same build target different backend deployments.
VITE_API_BASE_URL=http://localhost:3000
